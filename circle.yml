general:
  branches:
    only:
      - master
machine:
  node:
    version: 5.0.0
dependencies:
  pre:
    - npm install    
deployment:
  production:
    tag: /v\d.*/
    commands:      
      - npm build
  pages:
    tag: /reusablemodule.*/
    commands:
      - ./ci/github-page.sh