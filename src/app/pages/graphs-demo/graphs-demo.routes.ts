import { Routes } from '@angular/router';
import { GraphsDemoComponent } from './graphs-demo.component';

export const GraphsDemoRoutes: Routes = [
  { path: 'graphs-demo',  component: GraphsDemoComponent }
];

