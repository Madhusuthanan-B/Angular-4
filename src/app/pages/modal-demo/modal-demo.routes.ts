import { Routes } from '@angular/router';
import { ModalDemoComponent } from './modal-demo.component';

export const ModalDemoRoutes: Routes = [
  { path: 'modal',  component: ModalDemoComponent }
];

