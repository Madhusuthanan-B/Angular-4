import { Routes } from '@angular/router';
import { FocusSetterComponent } from './focus-setter.component';

export const FocusSetterRoutes: Routes = [
  { path: 'focus-setter',  component: FocusSetterComponent }
];

